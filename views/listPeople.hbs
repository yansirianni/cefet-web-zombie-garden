<p>Ha {{people.length}} pessoa(s) registrada(s) no jardim</p>
{{#if people}}
  <table class="table table-striped table-hover">
    <thead>
      <tr>
        <th>id</th>
        <th>Nome</th>
        <th>Situacao</th>
      </tr>
    </thead>
    <tbody>
      {{#each people}}
        <tr>
          <td>{{person.id}}</td>
          <td>{{person.name}}</td>
          <td>
            {{#if person.alive}}
              Vivo
            {{else}}
              Comido por {{zombie.name}}
            {{/if}}
          </td>
        </tr>
      {{/each}}
    </tbody>
  </table>
{{/if}}
