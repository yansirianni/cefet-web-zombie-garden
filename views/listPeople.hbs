<h1>Pessoas que frequentam o Jardim</h1>
<hr>
<p>Ha {{people.length}} pessoa(s) registrada(s) no jardim</p>
{{#if people}}
  <table class="table table-striped table-hover">
    <thead>
      <tr>
        <th>id</th>
        <th>Nome</th>
        <th>Situacao</th>
        <th>Acao</th>
      </tr>
    </thead>
    <tbody>
      {{#each people}}
        <tr>
          <td>{{person.id}}</td>
          <td>{{person.name}}</td>
          <td>
            {{#if person.alive}}
              Vivo
            {{else}}
              Comido por {{zombie.name}}
            {{/if}}
          </td>
          <td><a href="/people/{{person.id}}?_method=DELETE">Excluir</a></td>
        </tr>
      {{/each}}
    </tbody>
  </table>
{{/if}}
